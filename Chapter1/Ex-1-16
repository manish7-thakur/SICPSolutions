(define (fast-exp b n)
    (define (fast-iter b n a)
        (cond ((= n 0) a)
               ((even? n) (fast-iter (* b b) (/ n 2) a))
                (else (fast-iter  b (- n 1) (* a b)))
                ))
                (fast-iter b n 1))